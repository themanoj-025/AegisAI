fastapi>=0.115.0
uvicorn[standard]>=0.53.0
pydantic-settings>=2.6.0
python-dotenv>=1.2.3

slowapi>=0.1.10,<0.2

# Prompt 2 dependencies
PyJWT[crypto]>=2.14.0
httpx>=0.28.1

# Prompt 3 dependencies
rq>=2.12.0,<3.0
redis>=8.1.0

# Prompt 4 dependencies
anthropic>=1.6.0
openai>=3.13.0
tenacity>=9.1.4

# Observability
prometheus-client>=0.26.0

# OpenTelemetry distributed tracing (optional, enabled via OTEL_ENABLED=true)
opentelemetry-api>=1.20.0,<2.0
opentelemetry-sdk>=1.20.0,<2.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0,<2.0
opentelemetry-instrumentation-fastapi>=0.41b0,<1.0

# Transitive deps pinned to floor versions with HIGH CVE fixes (Trivy, 2026-09-17):
# - jaraco.context <6.1.0: CVE-2026-23949 (path traversal)
# - wheel <0.46.2: CVE-2026-24049 (privilege escalation / arbitrary code)
jaraco.context>=6.1.2
wheel>=0.48.0
